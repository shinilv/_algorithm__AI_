/*
题意： 给你一个只包含正整数的非空数组 nums，判断是否可以将这个数组分割成两个子集，
使得两个子集的元素和相等。

数据范围：
1 <= nums.length <= 200
1 <= nums[i] <= 100

思路：
*/

class Solution {
public:
    bool canPartition(vector<int>& nums) {
        
    }
};
